
import { Difficulty, Topic } from './types';

export const DIFFICULTIES: Difficulty[] = [Difficulty.Easy, Difficulty.Medium, Difficulty.Hard];
export const TOPICS: Topic[] = [Topic.Mixed, Topic.Arithmetic, Topic.Fractions, Topic.Geometry, Topic.WordProblems, Topic.Logic];
export const XP_PER_PROBLEM = 15;
